// Generated by CoffeeScript 1.4.0
(function() {

  $(document).ready(function() {
    var $particle, $queryTextbox, watermark, width;
    watermark = "Particle name...";
    $queryTextbox = $("#queryTextbox");
    if ($queryTextbox.val().length === 0) {
      $queryTextbox.val(watermark).addClass("watermark");
    }
    $("#queryTextbox").blur(function() {
      if ($queryTextbox.val().length === 0) {
        return $queryTextbox.val(watermark).addClass("watermark");
      }
    }).focus(function() {
      if ($queryTextbox.hasClass("watermark")) {
        return $queryTextbox.val("").removeClass("watermark");
      }
    });
    $("#searchDiv").submit(function() {
      if ($queryTextbox.hasClass("watermark")) {
        $queryTextbox.val("");
      }
      return $("#groupCombo option").eq(0).text("");
    });
    $particle = $(".particle");
    if ($particle.length > 0) {
      width = 3 * $particle.first().outerWidth(true);
      $("#main").width(width);
      $('#searchDiv').width(width);
    }
    $("#groupCombo").change(function() {
      return $("#searchForm").submit();
    });
    return $("#groupCombo option").eq(0).text("All particles");
  });

}).call(this);
